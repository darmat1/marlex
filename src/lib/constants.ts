/** Default brand colors for a new project, used as the fallback wherever a project/slide doesn't set its own. */
export const DEFAULT_BG_COLOR = '#9B6140';
export const DEFAULT_ACCENT_COLOR = '#D1B852';
